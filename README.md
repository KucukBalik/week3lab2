POST and GET functions are simulated by using classes.
Products are added and displayed on Swagger UI.
According to different endpoints Products can be added by bulk or indivudially.

If user enters unvalid object values, error is displayed on Swagger UI.
Objects are checked by using @Valid to make sure added Products are valid.